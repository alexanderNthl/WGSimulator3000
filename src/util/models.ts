export type velocity = {
  x: number;
  y: number;
};

export type dimension = {
  w: number;
  h: number;
};

export type coordinate = {
  x: number;
  y: number;
};

export type asset = {
  id: string;
  path: string;
};
